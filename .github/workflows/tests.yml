name: Tests

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          path: hwpx-mcp-server
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          repository: airmang/python-hwpx
          ref: bf1f52cb380962ffab397ad916538eed7fcba505
          path: python-hwpx
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m pip install -e ../python-hwpx
        working-directory: hwpx-mcp-server
      - run: python -m pip install -e ".[test]"
        working-directory: hwpx-mcp-server
      - run: python scripts/check_public_hygiene.py
        working-directory: hwpx-mcp-server
      - run: ruff check --select E9,F .
        working-directory: hwpx-mcp-server
      - run: python -m pytest -q
        working-directory: hwpx-mcp-server
      - run: |
          python scripts/render_tool_contract.py --check --skip-skill
          python scripts/render_contract_delta.py --check
        working-directory: hwpx-mcp-server

  clean-package-install:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: base
            extras: ""
            markitdown: absent
          - label: all-runtime-extras
            extras: "[hwp,http,ingest,oracle,vision]"
            markitdown: present
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          path: hwpx-mcp-server
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          repository: airmang/python-hwpx
          ref: bf1f52cb380962ffab397ad916538eed7fcba505
          path: python-hwpx
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.12"
      - name: Build exact core and server wheels
        run: |
          python -m pip install build
          python -m build --wheel --outdir candidate-wheelhouse python-hwpx
          python -m build --wheel --outdir candidate-wheelhouse hwpx-mcp-server
          python hwpx-mcp-server/scripts/check_public_hygiene.py
      - name: Install wheel in a clean environment
        working-directory: hwpx-mcp-server
        env:
          EXTRAS: ${{ matrix.extras }}
          EXPECT_MARKITDOWN: ${{ matrix.markitdown }}
        run: |
          python -m venv .clean-package
          CORE_WHEEL=$(find ../candidate-wheelhouse -name 'python_hwpx-*.whl' -print -quit)
          SERVER_WHEEL=$(find ../candidate-wheelhouse -name 'hwpx_mcp_server-*.whl' -print -quit)
          .clean-package/bin/python -m pip install "${CORE_WHEEL}"
          .clean-package/bin/python -m pip install "${SERVER_WHEEL}${EXTRAS}"
          .clean-package/bin/python -m pip check
          .clean-package/bin/python - <<'PY'
          import importlib.util
          import os
          from importlib.metadata import metadata

          requirements = metadata("hwpx-mcp-server").get_all("Requires-Dist") or []
          assert not any(item.casefold().startswith("modelcontextprotocol") for item in requirements)
          found = importlib.util.find_spec("markitdown") is not None
          assert found is (os.environ["EXPECT_MARKITDOWN"] == "present")
          assert importlib.util.find_spec("hwpx_mcp_server.practice") is None
          import hwpx_mcp_server.server
          PY
